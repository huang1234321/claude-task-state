{
  "identifier": "default",
  "description": "Main window: app IPC (get_sessions etc.) + frontend-initiated window drag/hide/show/close.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-hide",
    "core:window:allow-show",
    "core:window:allow-close"
  ]
}
